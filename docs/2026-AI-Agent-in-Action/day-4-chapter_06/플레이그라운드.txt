git clone https://github.com/cxbxmxcx/GPTAssistantsPlayground
cd GPTAssistantsPlayground
conda create -n env1 python=3.9
conda activate env1
pip install -r requirements.txt

# 1. 기존 버전 삭제
pip uninstall huggingface_hub -y

# 2. 교재 호환 버전 (v0.24.x) 설치
pip install "huggingface_hub==0.25.2"

python -c "from huggingface_hub import HfFolder; print('✅ HfFolder 임포트 성공!')"

pip install peewee moviepy


pip install moviepy imageio[ffmpeg] imageio[pyav]

pip install moviepy


# 1. 일단 꼬여있는 구형 Prefect를 완전히 제거
pip uninstall prefect -y

# 2. 코드 문법(from prefect import flow)에 맞는 v2.x 최신 계열 설치
# 파이썬 3.9 환경에서도 아주 잘 돌아가는 버전입니다.
pip install "prefect>=2.0.0,<3.0.0"

# 3. 설치 후 즉시 확인 (이게 통과되어야 모든 파일의 에러가 사라집니다)
python -c "from prefect import flow; print('✅ 모든 파일의 Prefect 에러 해결!')"



# --- 핵심 프레임워크 & UI ---
gradio==4.44.1
gradio-client==1.3.0
fastapi>=0.112.0
uvicorn>=0.30.0

# --- Pydantic v1/v2 호환성 레이어 (가장 중요) ---
pydantic>=2.0.0
pydantic-settings>=2.0.0
# Rudy 에이전트가 v1을 사용한다면 내부에서 pydantic.v1을 참조하게 설계됨

# --- LLM SDKs ---
openai>=1.40.0
groq>=0.9.0
anthropic>=0.34.0
tiktoken>=0.7.0

# --- 에이전트 워크플로우 & 로직 ---
prefect>=2.19.0
py-trees==2.2.3
jinja2>=3.1.0

# --- 검색 & 데이터 소스 ---
wikipedia>=1.4.0
twitter-api-client>=0.10.0
youtube-transcript-api>=0.6.0
googlemaps>=4.10.0
azure-search-documents==11.4.0

# --- 미디어 & 파일 처리 ---
python-pptx>=0.6.21
python-docx>=0.8.11
markdown>=3.5.0
moviepy>=1.0.3
opencv-python>=4.8.0
scipy>=1.10.0
pymupdf>=1.23.0

# --- 자동화 & 외부 서비스 ---
fal-client>=0.4.0
pyautogui>=0.9.54

# --- 유틸리티 ---
python-dotenv>=1.0.0
pytest>=7.4.0
typing-extensions>=4.8.0